version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Using Node $(node -v) and npm $(npm -v)"
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
